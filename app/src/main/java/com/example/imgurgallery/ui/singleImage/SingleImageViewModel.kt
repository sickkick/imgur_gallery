package com.example.imgurgallery.ui.singleImage

import androidx.lifecycle.ViewModel
import com.example.imgurgallery.data.search.SearchRepository
import javax.inject.Inject

class SingleImageViewModel @Inject constructor(private val repository: SearchRepository) : ViewModel() {
}